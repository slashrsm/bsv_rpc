language: elixir

elixir:
  - 1.9

otp_release:
  - 22.0

sudo: false

env:
  - MIX_ENV=test

install:
  - mix local.hex --force
  - mix local.rebar --force
  - mix deps.get

script:
  - mix check
  - mix coveralls.travis
